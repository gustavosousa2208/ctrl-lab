/*
 * Generated by CMake from a committed test fixture - do not edit.
 *
 * The plan is embedded rather than received over a transport because a
 * transport is stage E. Stage D's question is whether the runtime computes the
 * right numbers, and a linked-in plan answers it without a second unknown.
 *
 * Source plan : @CTRL_PLAN@.plan.dcp
 * Step count  : taken from @CTRL_PLAN@.f32.csv, so the device runs exactly as
 *               many ticks as the reference trace it is graded against.
 */

#ifndef CTRL_PLAN_BLOB_H
#define CTRL_PLAN_BLOB_H

#include <stdint.h>

#define CTRL_PLAN_NAME  "@CTRL_PLAN@"
#define CTRL_PLAN_STEPS @CTRL_PLAN_STEPS@

static const uint8_t ctrl_plan_blob[] = {
@CTRL_PLAN_BYTES@
};

#endif /* CTRL_PLAN_BLOB_H */
