node_modules
frontend/node_modules
dist
frontend/dist
backend/target
.DS_Store
*.local
.codex

# Generated by `tsc -b` in frontend/ — the sources are vite.config.ts and the
# tsconfig files. A committed vite.config.js would shadow vite.config.ts,
# because Vite resolves .js before .ts.
*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts

# Local dev-server logs
.tauri-dev.err
.tauri-dev.log
.tauri-check.err
.tauri-check.log
.vite-dev.err
.vite-dev.log

# The natively-built firmware host harness (firmware/ctrl/host/build.sh).
# Zephyr build trees live outside the repo entirely - see firmware/scripts/.
firmware/ctrl/host/ctrl-host
