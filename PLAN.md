# Current Plan

- [x] you divide the top bar into two sections, both left aligned, with a visible subtle vertical bar indicating, the you change only the simulation related things to the second section, and you make sure the editor related things are always on the left
- [x] when using shift select then delete, delete all the selected items one time, not one by one
- [x] make the fonts of indicators on top bar and bottom bar use the same font and size as their name, still "end time" and displayed value keeps different

If an item needs rework, remove its `[x]` and specify what to reexamine.
